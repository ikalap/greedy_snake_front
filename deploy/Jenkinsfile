pipeline {
    agent any

    stages {
        stage('Build & Deploy') {
            steps {
                sh 'ls'
                sh 'docker-compose up -f deploy/docker-compose.yml -d'
            }
        }

        stage('Finish') {
            steps {
                sh 'echo "部署成功"了'
            }
        }
    }
}
